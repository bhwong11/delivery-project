<div class="board-body">
    <div class="board-nav">
        <h3>Channels</h3>
        <% boards.forEach(board => { %>
            <div class="single-board">
                <img src="https://picsum.photos/50/50">
                <a href="/boards/<%= board.id %>"><%= board.name %></a>
            </div>
        <% }) %>
        <a href="/boards/new"><div class="new-board">+</div></a>
    </div>
    
    <div class="post-container">
        <h2><%= board.name %></h2>
        <p>Recent Posts</p>
        <div class=new-post-row>
            <a href="/posts/new" class="create-post"><i class="fas fa-comment-dots"></i> Create a Post</a> 
            <i class="fas fa-filter"></i>
        </div>
        <% posts.forEach(post => { %>
            <div class="single-post">
                <img src="<%= post.postImagePath %>" height=50 width=50>
                <div class="post-words">
                    <div><%= post.title %></div>
                    <div><%= post.content %></div>
                </div>
            </div>
        <% }) %>
    </div>
</div>
<%- include(`../partials/nav`) %>


